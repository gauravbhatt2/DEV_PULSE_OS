# DevPulse Agent OS — Backend Dependencies
# Unified requirements from devpulse + DevPlus-OS

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database — PostgreSQL (sync)
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Settings management
pydantic-settings==2.2.1

# HTTP client (async API calls to GitHub, Jira, Groq)
httpx==0.27.0

# GitHub App Authentication (JWT with RS256)
PyJWT==2.8.0
cryptography==42.0.8

# Environment
python-dotenv==1.0.1
